   $70 \le v_{FY_1} \le 140  \text{(无人机速度约束)} $
    $ 0 \le \alpha_1 < 2\pi  \text{(无人机航向约束)} $
   $ t_{drop,1,k} \ge 0  \text{(投放时间非负约束)} ,k=1,2,3$
    $t_{fuse,1,k} \ge 0  \text{(引信时间非负约束)},k=1,2,3 $
    $z_{det,1,k}(X) > 0  \text{(起爆高度物理约束)},k=1,2,3$

   $ t_{drop,1,k} \ge t_{drop,1,k-1}+1  \text{(相邻投放时间相差一秒以上)} k=2,3$

​    $t_{det,1,k} \le 13.943$

修正有效遮蔽的条件：由于此时有多个球状烟幕，之前的以真目标的上下两个圆周均在某个遮蔽锥内的判定方法从充要条件变为充分条件，现在的遮蔽条件改为，时间t时，为对于圆柱上任意点Q，均存在某个遮蔽锥使其位于遮蔽锥内，则认为时间t为有效遮蔽时刻。


好的，队长。遵照您的指示，我将之前整合好的模型约束条件，以标准的Markdown格式输出，方便您直接复制到论文或报告中。

---

### 问题三：模型约束条件

#### 决策变量

该问题的核心是寻找一组最优的决策变量，使得总遮蔽时间最长。决策变量共8个：

* **无人机飞行策略 (2个)**:
    * $v_{FY_1}$: 无人机的飞行速度。
    * $\theta_{FY_1}$: 无人机的飞行航向角。
* **三枚干扰弹的投放策略 (6个)**:
    * $t_{drop,1,1}, t_{drop,1,2}, t_{drop,1,3}$: 三枚干扰弹的投放时刻。
    * $t_{fuse,1,1}, t_{fuse,1,2}, t_{fuse,1,3}$: 三枚干扰弹各自的引信时长。

#### 约束条件列表

**1. 无人机飞行约束**

* **速度约束**: 无人机飞行速度大小必须在规定区间内。
    $$
    70 \le v_{FY_1} \le 140
    $$
* **航向角约束**: 根据您的MATLAB代码，航向角的搜索范围被限定在一个较小的区间内。
    $$
    0 \le \theta_{FY_1} \le 0.2 \quad (\text{rad})
    $$

**2. 干扰弹投放与引信约束**

* **投放时间约束**: 根据您的MATLAB代码，每次投放必须在任务开始后的10秒内完成。
    $$
    0 \le t_{drop,1,k} \le 10 \quad (\text{for } k=1,2,3)
    $$
* **引信时长约束**: 根据您的MATLAB代码，每枚弹的引信时长也被限制在10秒内。
    $$
    0 \le t_{fuse,1,k} \le 10 \quad (\text{for } k=1,2,3)
    $$
* **最小投放间隔约束** (源于题目核心要求 $\Delta t_{drop\_min}$): 任意两次投放的时刻间隔必须不小于1秒。
    $$
    |t_{drop,1,k_1} - t_{drop,1,k_2}| \ge \Delta t_{drop\_min} = 1 \quad (\text{for any } k_1 \neq k_2)
    $$

**3. 物理与任务可行性约束**

* **起爆高度约束**: 所有干扰弹必须在地面以上爆炸。设无人机初始高度为 $z_{FY_1}(0)$。
    $$
    z_{FY_1}(0)  - \frac{1}{2}g \cdot (t_{fuse,1,k})^2 \ge 0 \quad (\text{for } k=1,2,3)
    $$
  

* **任务有效性约束**: 所有干扰弹的起爆时刻 $t_{det,1,k} = t_{drop,1,k} + t_{fuse,1,k}$ 必须在导弹M1击中假目标之前。
    $$
    t_{drop,1,k} + t_{fuse,1,k} < T_{impact, M_1} \quad (\text{for } k=1,2,3)
    $$
    其中, $T_{impact, M_1} = \frac{\|\vec{P}_{M_1}(0)\|}{v_M}$。